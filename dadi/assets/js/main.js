/*
Mail:
    - Chiedi all’utente la sua email,
    - controlla che sia nella lista di chi può accedere,
    - stampa un messaggio appropriato sull’esito del controllo.
*/

// Chiedi all'utente la sua email

// Metto un input sul DOM e lo collego a main.js

// Creo la condizione attraverso la quale il value dell'input corrisponda oppure no alla lista

// Quando il value dell'input corrisponde alla condizione stampo un messaggio sull'esito del controllo

const input = document.getElementById(`email`).value;

console.log(input)